# 기여 가이드 (Contributing)

- Ondal 기여 방법 안내 문서
- 코드 외 기여도 전부 기여로 인정: 문제 출제, 문서, 디자인, QA

## 프로젝트 구조

```
ondal/
├─ backend/          # Spring Boot API 서버
├─ frontend/         # 웹 프론트엔드 (조직 컨벤션 스택)
├─ docs/
│  ├─ decisions/     # 의사결정 기록 - "왜 이렇게 만들었는가"
│  ├─ meetings/      # 회의록
│  └─ screens/       # 화면 정의, Figma 링크
└─ .github/          # 이슈/PR 템플릿, CI
```

## 프로젝트 개요

- 본체: 부트캠프 과제 제출·관리 플랫폼
- 온라인 저지(OJ): 그 위에 얹는 추가 기능
- 핵심 설계: **OJ 문제 = 기수 없음 + 마감 없음 + 자동채점인 과제** → 시스템 이중 구현 방지

## 기술 선택 배경

- 각 선택의 배경: [docs/decisions/](docs/decisions/)에 기록
- 새로운 기술적 결정도 같은 형식으로 기록
- 기록 기준: "초기 개발 참여 인원이 모두 사라졌을 때"에도 프로젝트가 이해 가능할 것

## 기여 절차

1. **이슈 확인** - 모든 작업은 이슈에서 시작. 첫 기여는 `good first issue` 라벨 참고
2. **브랜치 생성** - `main`에서 분기. 이름: `타입/이슈번호-짧은설명` (예: `feat/12-google-login`)
3. **작업 후 PR** - PR 템플릿에 맞춰 작성. `main` 직접 push 차단됨
4. **머지** - Squash merge 사용

## 커밋 메시지

```
타입: 요약 (50자 이내)

타입: feat / fix / docs / refactor / test / chore
예시: feat: 과제 제출 API 추가
```

## 문서 작성 규칙 (3개 레포 공통)

- 모든 문서는 **개조식**으로 작성
  - 종결: 명사형·체언 종결("~함", "~됨", "~임", 명사구) - 서술형("~한다", "~입니다") 지양
  - 산문 문단 금지: 2문장 이상 이어지는 문단은 목록·표로 분해, 한 항목 = 한 정보
- 문단 부호 적극 사용: `-` 불릿, `1.` 번호, `**강조**`, 표, `>` 인용, `→`, `:`, `※`
- 키보드 밖 특수 부호 금지: em dash·en dash·말줄임표(한 글자)·절 기호(section sign) → `-`, `...`, `N절`로 표기
- 허용 부호: `·`(가운뎃점), `→`, `※`, `①~⑩`, 이모지(문서 장식)
- 예외: 회의록(발언 기록 원문), 코드 블록·인라인 코드, 에러 메시지 등 원문 인용

## 규칙

- AI 적극 사용 가능 - 단, **본인이 올린 코드에 대해 질문받으면 이유를 설명할 수 있어야 함**
- 이해하기 어려운 코드: 리뷰에서 수정·설명 요청 가능
- 시크릿(비밀번호, API 키, OAuth 클라이언트 시크릿) 커밋 절대 금지 - `.gitignore`에 걸리는 파일 강제 추가 금지
- 사용자 개인정보를 다루는 기능 추가 시: 명시적 고지 필수

## 질문 창구

- 이슈 등록 또는 해달 디스코드 문의
- 어떤 질문이든 환영
