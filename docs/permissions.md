# 권한 모델 (Permissions)

> 확정: 2026-08-14 · 결정 기록: [decisions/0003](decisions/3-%EA%B6%8C%ED%95%9C-%EB%AA%A8%EB%8D%B8-2%EC%B8%B5-%EA%B5%AC%EC%A1%B0.md)

## 1. 두 층 구조

- 권한 구분 기준: "등급"이 아니라 **"범위"**
- 서로 다른 축의 정보 2개를 분리 저장

| 층 | 저장 위치 | 값 | 의미 |
|---|---|---|---|
| 전역 역할 | `User.global_role` | `ADMIN` / `MEMBER` | 동아리 임원인가 - **사람에게** 붙는 속성 |
| 분반 역할 | `Enrollment.role` | `OPERATOR` / `STUDENT` | **이 분반에서** 무엇인가 - 사람-분반 **관계에** 붙는 속성 |

교육 운영진을 전역 등급으로 만들지 않는 이유:

- 분반은 학기마다 자원자가 있어야 개설, 같은 사람이 C반에서는 운영진·Spring반에서는 수강생일 수 있음
  - "이 사람은 교육 운영진인가?" → 답 없음. "**이 분반의** 운영진인가?" → 답 있음
- 전역 슈퍼유저로 통합 시 문제 (최소권한 원칙 위반):
  - 남의 반 과제 조작 가능
  - 학기 종료 후에도 권한 잔존
  - 매 학기 명단 정리 작업 발생
- 분반에 묶인 OPERATOR: 분반 보관 처리 순간 운영(쓰기) 권한 자동 상실 - 열람은 유지
  - 권한 회수를 아무도 기억할 필요 없음
  - 구현: 보관 분반은 누구도 변경 불가, ADMIN은 보관 해제 후 변경. 상세: [guide/design.md](guide/design.md) 1절·8절-3

## 2. 판정 순서 - 모든 API 공통

1. 로그인 여부 → 아니면 `401`
   - 1-1. 승인 상태(`users.status`, 2026-09-19 [결정 10](decisions/10-%EC%8A%B9%EC%9D%B8-%EA%B2%8C%EC%9D%B4%ED%8A%B8-%EC%B2%AB-%EB%A1%9C%EA%B7%B8%EC%9D%B8%EC%9D%80-%EC%8A%B9%EC%9D%B8-%EB%8C%80%EA%B8%B0.md)) → `PENDING` 이면 `403 USER_PENDING` (`GET /api/auth/me` 만 예외 - `@PendingAllowed`). 역할이 있어도 승인 전엔 막힌다
2. `global_role == ADMIN` 여부 → 맞으면 **통과** (임원은 모든 분반에서 운영자 이상)
3. 해당 분반에 대한 Enrollment 존재 여부 → 없으면 `403`
4. `Enrollment.role`의 API 요구 수준 충족 여부 → 아니면 `403`

- 구현: 공통 컴포넌트(어노테이션) 하나로 4단계를 모든 API에 적용
- 개별 API에서 권한 로직 별도 작성 금지
- 고위험 영역 → PM 직접 구현

※ 보관(ARCHIVED) 분반에 대한 쓰기 거부는 이 4단계 밖 - 권한이 아니라 도메인 규칙:

- 응답: `409 COHORT_ARCHIVED` (403 아님)
- 상세: [guide/design.md](guide/design.md) 2절

## 3. 권한 매트릭스

| 기능 | 임원진<br>(전역 ADMIN) | 교육 운영진<br>(그 분반 OPERATOR) | 교육생<br>(그 분반 STUDENT) | 미소속 부원 |
|---|---|---|---|---|
| 분반 생성·보관, 운영진 지정 | ✅ | ❌ | ❌ | ❌ |
| 수강생 배정·제외 | ✅ 모든 반 | ✅ 자기 반 | ❌ | ❌ |
| 과제 CRUD·마감 설정 | ✅ | ✅ | ❌ | ❌ |
| 제출물 전체 열람·미제출 대시보드·제출 코멘트 남기기·지우기 | ✅ | ✅ | ❌ | ❌ |
| 과제 열람·제출 | ✅ | ✅ | ✅ | ❌ |
| 본인 제출 기록·코멘트 열람 | - | - | ✅ 본인 것 | ❌ |
| Q&A 질문 열람·등록 | ✅ | ✅ | ✅ | ❌ |
| Q&A 질문 수정 | ✅ 본인 것 | ✅ 본인 것 | ✅ 본인 것 | ❌ |
| Q&A 질문 삭제 | ✅ 모든 반 | ✅ 자기 반 | ✅ 본인 것 | ❌ |
| Q&A 답변 열람·등록 | ✅ | ✅ | ✅ | ❌ |
| Q&A 답변 수정 / 삭제 | ✅ 본인 것 / ✅ 모든 반 | ✅ 본인 것 / ✅ 자기 반 | ✅ 본인 것 / ✅ 본인 것 | ❌ |
| 공지 열람 | ✅ 전부 | ✅ 전체 공지 + 자기 반 | ✅ 전체 공지 + 자기 반 | ✅ 전체 공지만 |
| 전체 공지 등록·수정·삭제 | ✅ | ❌ | ❌ | ❌ |
| 분반 공지 등록·수정·삭제 | ✅ 모든 반 | ✅ 자기 반 (작성자 무관) | ❌ | ❌ |
| 차시 목록·내 출석 열람 | ✅ | ✅ | ✅ 본인 기록 | ❌ |
| 차시 등록·수정·삭제, 차시 명부 열람·출석 표시 | ✅ 모든 반 | ✅ 자기 반 | ❌ | ❌ |
| 자동 채점 문제 출제 - 테스트케이스·제한 저장, 출제 검증 실행, 재채점 | ✅ 모든 반 | ✅ 자기 반 | ❌ | ❌ |
| 자동 채점 예시(공개 케이스)·내 채점 결과 열람 | ✅ | ✅ | ✅ 본인 제출 | ❌ |
| 분반 목록 보기 | ✅ | ✅ | ✅ | ✅ (미소속 안내) |
| (P3) OJ 문제 출제 | ✅ | ❌ | ❌ | ❌ |
| (P3) OJ 문제 풀이 | ✅ | ✅ | ✅ | ✅ |

- Q&A 행(2026-09-09 추가): 근거는 [decisions/0006](decisions/6-qna-%EA%B2%8C%EC%8B%9C%ED%8C%90-p1-%ED%8E%B8%EC%9E%85.md). 어노테이션은 분반 소속까지, "본인 / 운영진" 판정은 서비스에서 `403` - [qna/design.md](qna/design.md) 결정 2
- 자동 채점 행(2026-09-14 설계, P2): 출제·검증·재채점은 `@CohortRole(OPERATOR)`, 예시 조회는 `@CohortRole(STUDENT)`. 채점 결과는 제출 응답(#19·#20·#22)에 실리므로 제출물 열람 권한과 동일 - [judge/design.md](judge/design.md)
- 제출 코멘트(2026-09-14 확정, P2): 위 "리뷰" 표기를 코멘트로 정리. `@CohortRole(OPERATOR)` 한 줄, 작성자 무관(운영진 팀이 함께 검토), 제출 1건에 1개 덮어쓰기, 점수 없음 - [submission/design.md](submission/design.md) 결정 18
- 출석 행(2026-09-14 추가, P2): 표시는 운영진만(셀프 체크인 없음), 학생은 자기 기록만 - [attendance/design.md](attendance/design.md) 결정 3·5
- 공지 행(2026-09-14 추가, P2): 쓰기 권한은 작성자가 아니라 "관리 권한" - 공지는 운영진 팀이 함께 관리하는 글. 전체 공지는 `@AdminOnly`, 분반 공지 등록은 `@CohortRole(OPERATOR)`, 수정·삭제는 공지 id 만으로 접근하므로 서비스에서 `CohortAuthorizer` 판정 - [notice/design.md](notice/design.md) 결정 2·3

## 4. 분반 생성과 부트스트랩

- 분반 생성·보관, OPERATOR 지정: **전역 ADMIN 전용**
  - 근거: OPERATOR는 분반이 있어야 존재 가능 → 분반 생성 권한은 전역일 수밖에 없음
- "강사 자원" 절차: 시스템 밖(동아리 의사결정)에서 진행, 시스템은 결과만 기록
  - ADMIN의 분반 생성 화면에서 이름·설명·운영진 지정 일괄 처리 (UC-A1)
- **최초 관리자(부트스트랩)**: 첫 배포 시 DB에 수동으로 `global_role = ADMIN` 지정 (대상 행은 홈페이지 계정으로 1회 로그인해 생성된 `users` 행 - `login_id` = Keycloak username, 결정 7). 절차는 운영 문서에 기록

## 5. 수강생 배정과 명부 연동

- P1: 운영진 직접 배정. 학생 신청 → 승인 흐름은 P1 제외 ([decisions/0004](decisions/4-%EC%B6%9C%EC%84%9D%EB%B6%80-qna-%EC%88%98%EA%B0%95%EC%8B%A0%EC%B2%AD-p1-%EC%A0%9C%EC%99%B8.md))
- ~~"명부에서 체크리스트로 배정" UX: 홈페이지의 **회원 명부 조회 API** 제공이 전제~~ → **2026-09-19 대체**: 홈페이지 API 없이 Ondal 의 부원 목록(`GET /api/users` - 홈페이지로 로그인해 생긴 계정 전부, 승인 상태·소속 요약 포함)에서 체크박스로 고른다 (결정 10, FE 명부 배정 모달)
- 준회원/정회원 등 회원 상태의 source of truth = 홈페이지
  - Ondal은 읽어서 표시만, 저장 안 함 (이중 저장 = 불일치)

## 6. OJ 모드 (P3)

- OJ 문제 = `cohort_id NULL` + `deadline NULL` + 자동채점인 Assignment
- 출제: 전역 ADMIN 전용 (분반 부재 → OPERATOR 개념 불성립)
- 풀이: 로그인한 부원 전체 - 분반 미소속 포함

## 문제 라이브러리(HOJ)·태그 (2026-09-15, V7)

문제는 분반에 속하지 않으므로 경로에 `{cohortId}` 가 없다 → 기존 `@CohortRole` 로 판정할 수 없어 **`@OperatorAnywhere`** 를 신설했다.

| 대상 | 판정 | 어노테이션 |
|---|---|---|
| 문제 조회(목록·상세·공개 예시) | 로그인한 누구나 - HOJ 는 분반 미소속 부원에게도 열린다 | `@LoginOnly` |
| 문제 출제·수정·삭제, 채점 설정·출제 도구 | ADMIN 이거나 **어느 분반에서든 운영진** | `@OperatorAnywhere` |
| 태그 등록·수정·삭제 | **전역 ADMIN 만** | `@AdminOnly` |
| 태그 조회 | 로그인한 누구나 | `@LoginOnly` |
| HOJ 연습 제출 | 로그인한 누구나 | `@LoginOnly` |
| 내 연습 제출 조회 | 본인만 - 남의 것은 404(존재 비노출) | `@LoginOnly` + 서비스에서 user 로 좁힘 |
| 과제 재채점 | 그 분반 운영진 이상 | `@CohortRole(OPERATOR)` |

- 태그만 ADMIN 으로 좁힌 이유: 운영진이 자유로 만들면 "DP / 다이나믹프로그래밍 / dp" 로 표기가 갈라져 분류가 쓸모없어짐. **선택은 운영진, 어휘 관리는 관리자**
- 출제 권한을 넓게(운영진 이상) 둔 근거: "출제는 극소수가 담당한다"(2026-09-15 PM) - 실제 사용자 수가 적어 좁힐 실익이 없음

## 부원 목록·승인 - 승인 게이트 (2026-09-19, [결정 10](decisions/10-%EC%8A%B9%EC%9D%B8-%EA%B2%8C%EC%9D%B4%ED%8A%B8-%EC%B2%AB-%EB%A1%9C%EA%B7%B8%EC%9D%B8%EC%9D%80-%EC%8A%B9%EC%9D%B8-%EB%8C%80%EA%B8%B0.md))

홈페이지(구글) 첫 로그인으로 생긴 계정은 `users.status = PENDING`(승인 대기)이다. 위 매트릭스의 어느 열에도 해당하지 않는다.

| 대상 | 판정 | 어노테이션 |
|---|---|---|
| 승인 대기 계정의 모든 API | `403 USER_PENDING` - 예외는 내 정보(`GET /api/auth/me`)와 공개 경로(로그아웃) | 인터셉터 공통 처리 + `@PendingAllowed`(me 에만) |
| 부원 목록 `GET /api/users` (소속 요약 포함, 승인 대기 먼저, `?status=`) | ADMIN 이거나 **어느 분반에서든 운영진** | `@OperatorAnywhere` |
| 승인 `POST /api/users/{id}/approve` (멱등) | 위와 같음 - 해구르르 또는 교육운영진 (PM "안 B") | `@OperatorAnywhere` |
| 분반 배정(수강생 배정·운영진 지정)에 딸린 **자동 승인** | 배정 권한 그대로 (`@CohortRole(OPERATOR)` / `@AdminOnly`) | 서비스에서 `user.approve()` |

- ACTIVE 가 되는 경로: 승인 API · 분반 배정 · 처음부터 ACTIVE 로 생긴 계정(명부 선등록 `findOrCreateMember`, 스텁 로그인, 부트스트랩 관리자)
- 운영진까지 승인 권한을 준 이유: 첫날 병목 방지. 승인 = "부원 확인" 수준이라 분반 배정과 같은 신뢰 수준
